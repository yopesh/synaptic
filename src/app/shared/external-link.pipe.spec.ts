import { ExternalLinkPipe } from './external-link.pipe';

describe('ExternalLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ExternalLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
